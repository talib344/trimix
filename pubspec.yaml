name: trimix
description: Trimix Video Editor
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.0
  file_picker: ^8.0.0
  ffmpeg_kit_flutter: ^6.0.3

flutter:
  uses-material-design: true